/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/netflix',
  assetPrefix: '/netflix',
}

module.exports = nextConfig